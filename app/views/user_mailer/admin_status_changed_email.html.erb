<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <p><%= t("email.admin_status_changed.body", id: @order.id) %>:</p>
    <p><%= t("email.admin_status_changed.new_status") %> <strong><%= t(@order.status.name) %></strong></p>
    <p><%= link_to t("email.admin_status_changed.link"), @url %></p>
  </body>
</html>