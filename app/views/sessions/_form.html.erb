<%= simple_form_for(:session, url: sessions_path, html: {class: 'form-vertical'}) do |f| %>
  <%= f.input :email %>
  <%= f.input :password, label: t("Password") %>
  
  <%= f.button :submit, t('Sign in'), class: 'btn btn-primary' %>
<% end %>