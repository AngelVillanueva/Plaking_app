<% provide :title, t('Sign in') %>

<h1><%= t("Sign in") %></h1>

<div class="row">
	<div class="span6 offset3">
		<%= render 'form' %>
		<p><%= t("New user?") %> <%= link_to t("Sign up now!"), signup_path %></p>
	</div>
</div>
