<ul class="unstyled">
  <% relevant_before.each do |key, value| %>
    <li><%= t(key) %>: <%= number_to_currency(value) %></li>
  <% end %>
  <li><%= t(:ivtm_line) %> : <%= number_to_currency(@ivtm) %></li>
  <% relevant_after.each do |key, value| %>
    <li><%= t(key) %>: <%= number_to_currency(value) %></li>
  <% end %>
  <li><%= t(:vat_line) %> : <%= number_to_currency(@vat_tax) %></li>
</ul>